-How does the warp aggregated atomic filter work slower than the normal atomic filter, as opposed to what this blog
claims: https://developer.nvidia.com/blog/cuda-pro-tip-optimized-filtering-warp-aggregated-atomics/

-Why is there a performance difference between vector add ILP maximization code and vector add vectorized load,
with the former working faster than the latter?
